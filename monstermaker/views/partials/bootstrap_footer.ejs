    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="javascripts/bootstrap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-slider/9.5.3/bootstrap-slider.min.js"></script>

    <!-- Slider functions -->
    <script type='text/javascript'>
		/* Stat 1 */
            var slider = new Slider("#stat1", {
				ticks: [1, 25, 50, 75, 100],
				ticks_labels: ['Terrible', 'Bad', 'Average', 'Good', 'Great'],
				ticks_snap_bounds: 30,
				value: 50
			});

		/* Stat 2 */
            var slider = new Slider("#stat2", {
				ticks: [1, 25, 50, 75, 100],
				ticks_labels: ['Terrible', 'Bad', 'Average', 'Good', 'Great'],
				ticks_snap_bounds: 30,
				value: 50
			});

		/* Stat 3 */
            var slider = new Slider("#stat3", {
				ticks: [1, 25, 50, 75, 100],
				ticks_labels: ['Terrible', 'Bad', 'Average', 'Good', 'Great'],
				ticks_snap_bounds: 30,
				value: 50
			});

		/* Stat 4 */
            var slider = new Slider("#stat4", {
				ticks: [1, 25, 50, 75, 100],
				ticks_labels: ['Terrible', 'Bad', 'Average', 'Good', 'Great'],
				ticks_snap_bounds: 30,
				value: 50
			});

		/* Stat 5 */
            var slider = new Slider("#stat5", {
				ticks: [1, 25, 50, 75, 100],
				ticks_labels: ['Terrible', 'Bad', 'Average', 'Good', 'Great'],
				ticks_snap_bounds: 30,
				value: 50
			});

		/* Stat 6 */
            var slider = new Slider("#stat6", {
				ticks: [1, 25, 50, 75, 100],
				ticks_labels: ['Terrible', 'Bad', 'Average', 'Good', 'Great'],
				ticks_snap_bounds: 30,
				value: 50
			});

		/* Stat 7 */
            var slider = new Slider("#stat7", {
				ticks: [1, 150, 300],
				ticks_labels: ['Light', 'Average', 'Heavy'],
				ticks_snap_bounds: 30,
				value: 150
			});



    </script>
